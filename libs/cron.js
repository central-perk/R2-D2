// Generated by CoffeeScript 1.7.1
var CronJob, backup, path, request, storage, store;

path = require('path');

storage = require(path.join(process.g.controllersPath, 'mapping', 'storage'));

CronJob = require('cron').CronJob;

request = require('request');

backup = new CronJob('*/2 * * * * *', function() {
  var timestamp;
  timestamp = (new Date()).getTime();
  return request.get('http://127.0.0.1:8001?_type=openLogin&timestamp=' + timestamp + '&openID=12345');
}, null, true, 'Asia/Shanghai');

store = new CronJob('*/5 * * * * *', function() {
  return storage();
}, null, true, 'Asia/Shanghai');
